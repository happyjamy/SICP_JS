function cube(x) {
    return x * x * x;
}
function p(x) {
    return 3 * x - 4 * cube(x);
}
function sine(angle) {
    return ! (abs(angle) > 0.1)
           ? angle
           : p(sine(angle / 3));
}

/*
1, How many times is the functionp applied when sine(12.15) is evaluated?
5

2, What is the order of growth in space and number of steps (as a function of aa) 
used by the process generated by the sinefunction when sine(a) is evaluated?
*/